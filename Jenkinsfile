pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'No build required'
            }
        }

        stage('Test') {
            steps {
                bat 'pytest'
            }
        }
    }
}
